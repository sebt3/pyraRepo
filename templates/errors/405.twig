{% extends "core.twig" %}
{% block title %}405 {{ _('not allowed') }}{% endblock %}
{% block contentHeader %}
        405
        <small>{{ _('not allowed') }}</small>
{% endblock %}
{% block content %}
    <section class="content">
      <div class="error-page">
        <h2 class="headline text-red"> 405</h2>

        <div class="error-content">
          <h3><i class="fa fa-ban text-red"></i> {{ _('Oops! Permission denied.') }}

          <p>{{ _("You're not allowed here.") }}</p>
	  <p>{{ _('Back to the') }} <a href="{{ path_for('home') }}">{{ _('Home') }}</a>.
          </p>
        </div>
      </div>
    </section>
{% endblock %}

