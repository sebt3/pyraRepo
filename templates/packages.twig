{% extends "core.twig" %}
{% block title %}{{ _('Packages') }}{% endblock %}
{% block contentHeader %}
        {{ _('Packages') }}
        <small>{{ _('list') }}</small>
{% endblock %}
{% block scripts %}
<script>
d3.select('section.content').call(repo.widgets.list().items(repo.widgets.items.pkg).data({{ json(packages) }}));
</script>
{% endblock %}
